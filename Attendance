
#include <stdio.h>

int main() {
    int n, i;

    printf("Enter the number of students: ");
    scanf("%d", &n);

    printf("Attendance numbers of students:\n");
    for(i = 1; i <= n; i++) {
        printf("Student %d\n", i);
    }

    return 0;
}
