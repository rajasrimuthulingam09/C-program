#include <stdio.h>
#include <string.h>

int main() {
    char password[50];

    printf("Enter password: ");
    scanf("%s", password);

    if(strlen(password) >= 8)
        printf("Password is valid");
    else
        printf("Password is invalid");

    return 0;
}
