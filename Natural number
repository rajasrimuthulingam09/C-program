#include <stdio.h>

int main() {
    int n, sum = 0, i;

    printf("Enter a positive number: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++) {
        sum += i; // Add numbers from 1 to n
    }

    printf("Sum of first %d natural numbers = %d\n", n, sum);

    return 0;
}
